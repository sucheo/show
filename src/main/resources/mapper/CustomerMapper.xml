<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.showppingmall.customer.CustomerMapper">

    <insert id="inscus">
        Insert into t_customer
        (nm,addr,ph)
        values
        (#{nm},#{addr},#{ph})

    </insert>

</mapper>